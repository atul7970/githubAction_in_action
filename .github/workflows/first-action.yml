name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello from Jack"
            echo "I've just watched odyessy"
      - name: Print Goodbye
        run: echo "Done Bye!!"
